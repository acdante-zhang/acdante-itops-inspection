# DESIGN.md

## 项目与用户画像
- DBA 和运维人员使用的 IT 运维巡检平台
- 需要快速巡检多类型设备、定位问题，操作要高效、信息要精准
- 支持企业数据中心、混合云环境

## Design Tokens

### 色彩
- 主色调：深色背景（slate-950/900），搭配青色(cyan)作为品牌色，琥珀色(amber)作为告警色
- 状态色标：
  - OK/正常 = 绿色 (green-400/500)
  - WARNING/告警 = 琥珀色 (amber-400/500)
  - CRITICAL/严重 = 红色 (red-400/500)
  - RUNNING/执行中 = 青色 (cyan-400/500)
  - PENDING/待执行 = 蓝色 (blue-400/500)
- 设备类型色标（标签）：
  - Linux = 系统默认
  - Oracle = 琥珀色
  - MySQL = 蓝色
  - PostgreSQL = 靛蓝色
  - Network = 青色
  - Storage = 绿色
  - BMC = 灰色
  - SAN Switch = 紫色

### 字体
- 系统字体栈（font-sans），等宽字体用于命令/SQL显示（font-mono）

## 布局与响应式
- 左侧固定侧边栏（240px）+ 右侧主内容区
- 侧边栏可折叠分组
- 表格数据密度高，行高紧凑
- 报告详情采用左右分栏（列表+详情）

## 交互规范
- 只读命令显示绿色锁图标，非只读命令显示琥珀色警告图标
- 连接测试使用旋转动画反馈
- 任务执行状态使用实时进度条
- 删除操作需二次确认

## HTTP 集成模块
- 证书状态色标：有效=绿色、即将过期(30天)=琥珀色、已过期=红色、自签名=蓝色
- TLS 版本标记：TLS 1.0/1.1 显示琥珀色警告（不安全），TLS 1.2=绿色，TLS 1.3=绿色
- 认证方式图标：表单=钥匙、Basic Auth=锁、Token=票券、OAuth=盾牌
- 设备连接测试使用实时状态动画（连接中→验证证书→登录→完成）
- 老设备兼容模式使用醒目橙色边框提示
